enum ContainerType { languageContainer, pageChangerContainer }

enum PasswordStates { show, hide }

enum DeviceStates { connected, disConnected }

enum DeviceConnectionState { blueTooth, wifi, notConnected }

enum ManagingSectionOptions { device, children, vehicles }

enum AlertSectionOptions { alertOption, notificationOption }

enum AppThemeModes { lightMode, darkMode, systemMode }

enum ShowCasePages {
  home,
  alert,
  sos,
  manage,
  profileSettings,
  emergency,
  biometric,
  batteryManaging,
  themeCustomization,
  preferences
}
